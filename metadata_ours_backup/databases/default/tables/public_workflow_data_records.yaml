table:
  schema: public
  name: workflow_data_records
object_relationships:
  - name: organization
    using:
      foreign_key_constraint_on: org_id
select_permissions:
  - role: user
    permission:
      columns: [id, org_id, workflow_run_id, step_run_id, key, value, created_at]
      filter:
        organization:
          org_members:
            user_id: { _eq: X-Hasura-User-Id }
